<Window x:Class="WpfApp2.Updater.UpdaterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="업데이트"
        Width="400" Height="190"
        Icon="/대시보드아이콘 3.ico"
        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="NoResize"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True">

    <Border Background="#FF1E1E1E"
            CornerRadius="10"
            BorderBrush="#FF3F3F46"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="270"
                              ShadowDepth="3" BlurRadius="15" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="30,25,30,25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Grid.Row="0"
                       Text="업데이트 설치 중"
                       FontSize="17" FontWeight="Bold"
                       Foreground="White"
                       Margin="0,0,0,10"/>

            <!-- Status -->
            <TextBlock Grid.Row="1"
                       x:Name="StatusText"
                       Text="앱 종료 대기 중..."
                       FontSize="13"
                       Foreground="#FFAAAAAA"
                       Margin="0,0,0,18"/>

            <!-- Progress bar with custom dark template -->
            <ProgressBar Grid.Row="2"
                         x:Name="ProgressBar"
                         IsIndeterminate="True"
                         Height="5"
                         Margin="0,0,0,0">
                <ProgressBar.Template>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="Root" ClipToBounds="True">
                            <Border Background="#FF3F3F46" CornerRadius="2.5"/>
                            <Border x:Name="GlowRect"
                                    Background="#FF4CAF50"
                                    CornerRadius="2.5"
                                    Width="80"
                                    HorizontalAlignment="Left">
                                <Border.RenderTransform>
                                    <TranslateTransform x:Name="MoveTransform" X="-80"/>
                                </Border.RenderTransform>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsIndeterminate" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimation
                                                Storyboard.TargetName="MoveTransform"
                                                Storyboard.TargetProperty="X"
                                                From="-80" To="400"
                                                Duration="0:0:1.4"
                                                AutoReverse="False"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </ProgressBar.Template>
            </ProgressBar>
        </Grid>
    </Border>
</Window>
