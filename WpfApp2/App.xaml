<Application x:Class="WpfApp2.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
                <!-- Removed Dark theme dictionary which was missing; can add if assembly actually contains it: MaterialDesign3.Dark.xaml -->
            </ResourceDictionary.MergedDictionaries>

            <!-- 기존 커스텀 색상 유지 -->
            <Color x:Key="WindowBackgroundColor">#FF232323</Color>
            <Color x:Key="ForegroundColor">#FFF1F1F1</Color>
            <Color x:Key="StatusBarBackgroundColor">#FF1A1A1A</Color>
            <Color x:Key="StatusBarBorderColor">#FF333333</Color>
            <Color x:Key="TitleBarButtonHoverColor">#FF161616</Color>
            <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}" />
            <SolidColorBrush x:Key="ForegroundBrush" Color="{StaticResource ForegroundColor}" />
            <SolidColorBrush x:Key="StatusBarBackgroundBrush" Color="{StaticResource StatusBarBackgroundColor}" />
            <SolidColorBrush x:Key="StatusBarBorderBrush" Color="{StaticResource StatusBarBorderColor}" />
            <SolidColorBrush x:Key="TitleBarButtonHoverBrush" Color="{StaticResource TitleBarButtonHoverColor}" />

            <!-- Dark ContextMenu theme (compact) -->
            <SolidColorBrush x:Key="ContextMenuBackgroundBrush" Color="#2C2C2C"/>
            <SolidColorBrush x:Key="ContextMenuBorderBrush" Color="#3A3A3A"/>
            <SolidColorBrush x:Key="ContextMenuForegroundBrush" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="ContextMenuItemHoverBrush" Color="#3A3A3A"/>
            <SolidColorBrush x:Key="ContextMenuItemPressedBrush" Color="#454545"/>
            <SolidColorBrush x:Key="ContextMenuSeparatorBrush" Color="#404040"/>
            <SolidColorBrush x:Key="DangerForegroundBrush" Color="#FF5A5A"/>
            <SolidColorBrush x:Key="DangerHoverBrush" Color="#662B2B"/>

            <!-- 탭 브러시 (런타임에 ThemeManager가 교체) -->
            <LinearGradientBrush x:Key="TabNormalBrush" StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#1D1D26" Offset="0"/>
                <GradientStop Color="#141419" Offset="1"/>
            </LinearGradientBrush>
            <LinearGradientBrush x:Key="TabHoverBrush" StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#252532" Offset="0"/>
                <GradientStop Color="#1A1A25" Offset="1"/>
            </LinearGradientBrush>
            <LinearGradientBrush x:Key="TabSelectedBrush" StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#2A2A3E" Offset="0"/>
                <GradientStop Color="#1C1C2E" Offset="1"/>
            </LinearGradientBrush>
            <LinearGradientBrush x:Key="TabAccentBrush" StartPoint="0,0" EndPoint="0,1">
                <GradientStop Color="#8BAAFF" Offset="0"/>
                <GradientStop Color="#5575EE" Offset="1"/>
            </LinearGradientBrush>
            <SolidColorBrush x:Key="TabTextNormalBrush"   Color="#8888A0"/>
            <SolidColorBrush x:Key="TabTextHoverBrush"    Color="#C8C8DC"/>
            <SolidColorBrush x:Key="TabTextSelectedBrush" Color="#FFFFFF"/>

            <Style TargetType="ContextMenu">
                <Setter Property="Background" Value="{DynamicResource ContextMenuBackgroundBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource ContextMenuBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Foreground" Value="{DynamicResource ContextMenuForegroundBrush}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ContextMenu">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="4" CornerRadius="8"
                                    SnapsToDevicePixels="True">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.35" Color="#000000"/>
                                </Border.Effect>
                                <StackPanel IsItemsHost="True"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="Separator">
                <Setter Property="Margin" Value="6,3"/>
                <Setter Property="Background" Value="{StaticResource ContextMenuSeparatorBrush}"/>
                <Setter Property="Height" Value="1"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Separator">
                            <Border Background="{TemplateBinding Background}" Height="{TemplateBinding Height}"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="MenuItem">
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="Foreground" Value="{DynamicResource ContextMenuForegroundBrush}"/>
                <Setter Property="Padding" Value="12,7"/>
                <Setter Property="MinHeight" Value="34"/>
                <Setter Property="Background" Value="{DynamicResource ContextMenuBackgroundBrush}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="MenuItem">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" SnapsToDevicePixels="True"
                                    MinHeight="{TemplateBinding MinHeight}" CornerRadius="4" Margin="2,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition x:Name="IconCol" Width="0"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter x:Name="IconPresenter" Grid.Column="0" ContentSource="Icon" Margin="0,0,6,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    <ContentPresenter Grid.Column="1" ContentSource="Header" RecognizesAccessKey="True" VerticalAlignment="Center"
                                                      Margin="{TemplateBinding Padding}"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ContextMenuItemHoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ContextMenuItemPressedBrush}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                                <!-- Collapse icon gutter when no icon to avoid left light strip -->
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Icon}" Value="{x:Null}">
                                    <Setter TargetName="IconCol" Property="ColumnDefinition.Width" Value="0"/>
                                    <Setter TargetName="IconPresenter" Property="UIElement.Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="danger">
                                    <Setter Property="Foreground" Value="{StaticResource DangerForegroundBrush}"/>
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource ContextMenuBackgroundBrush}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="danger"/>
                                        <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsHighlighted}" Value="True"/>
                                    </MultiDataTrigger.Conditions>
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource DangerHoverBrush}"/>
                                </MultiDataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="TitleBarButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{DynamicResource StatusBarBackgroundBrush}" />
                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}" />
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Width" Value="40"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource TitleBarButtonHoverBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
